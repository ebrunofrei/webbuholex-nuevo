# Normalización y ranking 11.M

Estado: aprobado. La normalización determinista afecta solo campos auxiliares y conserva el contenido público original. Los tokens se deduplican y ordenan.

El ranking validado es cerrado y explicable: expediente exacto, resolución exacta, título exacto, prefijo, términos, fecha e identificador estable. No usa IA, embeddings, RAG, similitud semántica, inferencia jurídica, reranking o servicio externo.

La corrección de exact optional properties no alteró normalización, ranking u ordenamiento.
